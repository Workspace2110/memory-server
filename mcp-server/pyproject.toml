[project]
name = "memory-server"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastmcp>=3.2.3",
    "pydantic>=2.12.5",
    "qdrant-client[fastembed]>=1.17.1",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0.0",
]
