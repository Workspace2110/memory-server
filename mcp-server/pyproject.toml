[project]
name = "memory-server"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastmcp>=2.0.0",
    "pydantic>=2.0.0",
    "qdrant-client[fastembed]>=1.9.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0.0",
]
