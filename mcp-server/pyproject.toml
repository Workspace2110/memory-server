[project]
name = "memory-server"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastmcp>=3.2.4",
    "pydantic>=2.13.2",
    "qdrant-client[fastembed]>=1.17.1",
]

[project.optional-dependencies]
dev = [
    "pytest>=9.0.3",
]
