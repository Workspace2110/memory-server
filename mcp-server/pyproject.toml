[project]
name = "memory-server"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastmcp>=3.4.7",
    "pydantic>=2.13.4",
    "qdrant-client[fastembed]>=1.18.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=9.1.1",
]
